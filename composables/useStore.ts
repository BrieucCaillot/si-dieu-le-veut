const isLoaderHidden = ref(false)
const resourcesLoaded = ref(false)
const ordalieCroix = ref(false)

export default () => {
  return {
    isLoaderHidden,
    resourcesLoaded,
    ordalieCroix,
  }
}
